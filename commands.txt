# pip install fastapi uvicorn python-docx reportlab
# pip install python-docx docx2pdf

# To run the server: uvicorn api:app --reload
#   If it doesn't work, this may work instead: python -m uvicorn main:app --reload

# {{company_name}}, {{date}}, {{hours_of_work}}, and {{hour_price}}, {{proof_of_work}}
